"""First-boot demo data.

The :func:`app.seeds.demo.seed_demo` entrypoint is called from the FastAPI
lifespan in :mod:`app.main` and can also be invoked directly via
``python -m app.seeds.demo``.
"""
